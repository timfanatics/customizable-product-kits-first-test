<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="sale_order_portal_content_inherit_bom_to_so" inherit_id="sale.sale_order_portal_content">
        <xpath expr="//t[@t-foreach='lines_to_report']" position="replace">
            <t t-foreach="lines_to_report" t-as="line">

                <t t-set="current_subtotal" t-value="current_subtotal + line.price_subtotal" groups="account.group_show_line_subtotals_tax_excluded"/>
                <t t-set="current_subtotal" t-value="current_subtotal + line.price_total" groups="account.group_show_line_subtotals_tax_included"/>
                <tr t-attf-class="#{'bom_head' if line.is_bom_head and not line.is_sub_kit else ''} #{'sub_kit' if line.is_sub_kit else ''} #{'bg-200 fw-bold o_line_section' if line.display_type == 'line_section' else ''} #{'fst-italic o_line_note' if line.display_type == 'line_note' else ''} #{'no_bom_product' if not line.is_bom_extracted else ''}">
                    <t t-if="not line.display_type">
                        <td id="product_name">
                            <span t-field="line.name"/>
                        </td>
                        <td class="text-end">
                            <div id="quote_qty">
                                <span t-field="line.product_uom_qty"/>
                                <span t-field="line.product_uom"/>
                            </div>
                        </td>
                        <td t-attf-class="text-end {{ 'd-none d-sm-table-cell' if report_type == 'html' else '' }}">
                            <div
                                t-if="line.discount &gt;= 0"
                                t-field="line.price_unit"
                                t-att-style="line.discount and 'text-decoration: line-through' or None"
                                t-att-class="(line.discount and 'text-danger' or '') + ' text-end'"
                            />
                            <div t-if="line.discount">
                                <t t-out="(1-line.discount / 100.0) * line.price_unit" t-options='{"widget": "float", "decimal_precision": "Product Price"}'/>
                            </div>
                        </td>
                        <td t-if="display_discount" t-attf-class="text-end {{ 'd-none d-sm-table-cell' if report_type == 'html' else '' }}">
                            <strong t-if="line.discount &gt; 0" class="text-info">
                                <t t-out="((line.discount % 1) and '%s' or '%d') % line.discount"/>%
                            </strong>
                        </td>
                        <td t-attf-class="text-end {{ 'd-none d-md-table-cell' if report_type == 'html' else '' }}">
                            <span t-out="', '.join(map(lambda x: (x.description or x.name), line.tax_id))"/>
                        </td>
                        <td t-if="not line.is_downpayment" class="text-end">
                            <span class="oe_order_line_price_subtotal" t-field="line.price_subtotal" groups="account.group_show_line_subtotals_tax_excluded"/>
                            <span class="oe_order_line_price_total" t-field="line.price_total" groups="account.group_show_line_subtotals_tax_included"/>
                        </td>
                    </t>
                    <t t-if="line.display_type == 'line_section'">
                        <td colspan="99">
                            <span t-field="line.name"/>
                        </td>
                        <t t-set="current_section" t-value="line"/>
                        <t t-set="current_subtotal" t-value="0"/>
                    </t>
                    <t t-if="line.display_type == 'line_note'">
                        <td colspan="99">
                            <span t-field="line.name"/>
                        </td>
                    </t>
                </tr>
                <tr t-if="current_section and (line_last or lines_to_report[line_index+1].display_type == 'line_section') and not line.is_downpayment"
                    class="is-subtotal text-end">
                    <td colspan="99">
                        <strong class="mr16">Subtotal</strong>
                        <span t-out="current_subtotal"
                            t-options='{"widget": "monetary", "display_currency": sale_order.pricelist_id.currency_id}'
                        />
                    </td>
                </tr>
            </t>
        </xpath>

        <xpath expr="//h3[@id='details']" position="attributes">
            <attribute name="class">d-inline-block</attribute>
        </xpath>

        <xpath expr="//h3[@id='details']" position="after">
            <div class="d-inline-block d_line_type">
                <label for="d_line_type" class="label">Display Type:</label>
                <select id="d_line_type" class="select_line_type">
                    <option value="heads">Main Products</option>
                    <option value="all">All Products</option>
                </select>
            </div>
        </xpath>

    </template>
</odoo>
